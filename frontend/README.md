# ValidProxy Frontend

Next.js 16 (App Router) + shadcn/ui + Tailwind CSS v4 前端，提供代理监控、筛选、排序与实时验证进度展示。

开发模式：`pnpm install && pnpm dev`，默认运行在 <http://localhost:5173>。

生产构建：`pnpm build`（standalone 输出，配合 Docker 使用）。

完整说明见仓库根目录 [README.md](../README.md)。
